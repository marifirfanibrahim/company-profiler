# core framework
flask>=3.0.0
flask-cors>=4.0.0

# haystack
haystack-ai>=2.0.0,<3  # 3.x removed OpenAIGenerator (backend/models/generator.py)
chroma-haystack>=4.5  # floor matches the locked chromadb 1.5.x pair
ollama-haystack>=1.0,<7  # <1.0 treats url as the full endpoint; 7.x removed OllamaGenerator and needs haystack-ai 3

# browser automation
selenium>=4.17.2

# ml models
transformers>=4.36.0,<5  # 5.x needs huggingface-hub>=1.3, see the huggingface-hub cap
torch>=2.6.0
sentence-transformers>=2.2.0,<6  # 6.x needs huggingface-hub>=1.3, see the huggingface-hub cap
gliner>=0.0.1
glirel>=0.0.1
huggingface-hub<1  # glirel 1.2.1 passes proxies/resume_download, which hub 1.x removed
datasets>=3  # 2.x subclasses pa.PyExtensionType, removed in pyarrow 25, breaking import sentence_transformers
numpy  # imported directly by backend/models/reranker.py

# data stores
chromadb>=1.5.4  # required by chroma-haystack 4.5

# content extraction
trafilatura>=1.6.0,<3  # 2.2.0 warns that no_fallback will be removed
lxml_html_clean>=0.2.0
pypdf>=4.0.0

# image ocr
pillow>=10.0.0
pytesseract>=0.3.10

# utilities
httpx>=0.25.2,<1  # 1.0 drops Limits, HTTPError, Client(verify=, limits=); openai uses the separate httpx2 package
feedparser>=6.0.10
python-dateutil>=2.8.2
beautifulsoup4>=4.12.0
nltk>=3.9.1  # haystack DocumentSplitter(split_by="sentence") needs punkt_tab
loguru>=0.7.0
apscheduler>=3.10.1

# environment
python-dotenv>=1.0.0